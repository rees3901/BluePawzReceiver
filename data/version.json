{
  "fs_version": "3.6.1",
  "fs_git_hash": "6ca5974-dirty",
  "_comment": "Filesystem version stamp. Bumped in lockstep with include/version.h's BLUEPAWZ_VERSION. fs_git_hash is auto-stamped by scripts/inject_git_hash.py at build time (this 'pending' placeholder is replaced on the next build). The /version HTTP endpoint serves both side-by-side so the web UI can detect firmware<>filesystem mismatch."
}
